services:
  botshub:
    build: .
    ports:
      - "4800:4800"
    volumes:
      - botshub-data:/app/data
    environment:
      - BOTSHUB_PORT=4800
      - BOTSHUB_PERSIST=true
      # - BOTSHUB_CORS=https://your-domain.com
    restart: unless-stopped

volumes:
  botshub-data:
